{
  "openapi": "3.1.0",
  "info": {
    "title": "{{项目名}} API",
    "version": "0.1.0",
    "description": "接口契约单一来源。先改本文件并通过评审，再实现；破坏性变更需升版本并给兼容期。"
  },
  "servers": [
    { "url": "http://localhost:8000", "description": "local" }
  ],
  "paths": {
    "/health": {
      "get": {
        "summary": "健康检查",
        "operationId": "getHealth",
        "responses": {
          "200": {
            "description": "服务正常",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "required": ["status"],
                  "properties": { "status": { "type": "string", "enum": ["ok"] } }
                }
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {}
  }
}
